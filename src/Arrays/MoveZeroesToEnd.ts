// Given an integer array nums, move all 0's to
// the end of it while maintaining the relative order of the non-zero elements.
export const moveZeroesToEnd = function (arr: number[]): number[] {};

// moveZeroesToEnd([0,1,0,3,12]);
